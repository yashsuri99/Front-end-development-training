// type - alias -> a short name for a type
type sn = string | number;
type Price = number;

let productPrice : Price;

let quantity: sn='10', score: sn = '95';

quantity = parseInt(quantity);
score = parseInt(score);

export{};