// Generally we don't give a data type explicitly
// if we give initial value for a variable, TypeScript "infers" the data type based on the value 
let greetings = 'Hello typescript';
let brandName: string = 'Cadbury';

let productName = 'Cadbury Eclair';

export {};