for( let i = 1; i <= 5; i++ ) {
    let line = '';

    for( let j = 1; i <= j; j++ ) {
        line = line + '* ';
    }

    console.log( line );
}

/*
    1. Goto the 4th column in vs
    2. Create a json file
    3. Again go there launch the program
    4. Enter breakpoints to debug it
*/